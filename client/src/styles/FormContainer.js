import tw, { styled, css } from "twin.macro";

const FormContainer = styled.div(() => [tw`w-full sm:max-w-sm p-4 sm:flex sm:flex-col sm:justify-center md:px-8 md:py-6`]);

export default FormContainer;
